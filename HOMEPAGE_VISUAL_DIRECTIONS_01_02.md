# 首页视觉方案 01–02

更新日期：2026-07-20  
用途：记录前两张视觉参考图的完整设计意图，作为后续选型与实现依据。  
范围：仅包含方案 01「机械编舞剧场」与方案 02「编辑电影切片」。本文不代表已经选定方向，也不授权修改生产页面。

> 选型结果（2026-07-21）：已选择方案 01，并深化为「后朋克机械装配线」。完整终版见 [`HOMEPAGE_FINAL_DESIGN_SPEC.md`](HOMEPAGE_FINAL_DESIGN_SPEC.md)。本文后续仅作为方案探索记录。

## 共同设计目标

两个方案都服务于同一个核心命题：**把 Agent 做成真正能工作的系统。** 页面不是被动陈列履历，而是让访问者亲眼看到一个 Agent 系统如何启动、组织上下文、调用工具、留下证据、通过边界校验，并最终达到可交付状态。

共同保留以下品牌 DNA：

- 近黑背景 `#11100e` / `#090907`，暖纸色 `#f0eadf`，安全橙 `#d75a32`。
- 重型中文展示字体负责结论，干净无衬线负责说明，等宽字体负责状态、编号和系统标签。
- 工业网格、坐标、刻度、裁切线、轨道、边界框和测量标记形成系统感。
- 橙色只标识当前状态、关键路径、告警或确认结果，不作为随意装饰。
- 页面内容必须真实，不虚构雇主、结果、奖项、日期、链接或项目结论。
- 无动画时仍应保持完整的阅读顺序、层级和导航能力。

---

## 方案 01：机械编舞剧场

### 一句话概念

把整张首页做成一台正在装配、运行和验收的 Agent 机器：首屏点火，中段沿轨道演示三个系统状态，时间日志持续吐出证据，结尾把所有零件锁入稳定边界。

### 整体气质

这是两个方案中最“系统工程化”的方向。画面像工程蓝图、控制台、接线图和验收面板的混合体，但不做成传统仪表盘。它的力量来自大标题与精密系统图的正面碰撞：左侧给出鲜明立场，右侧证明这套立场背后确实有结构。

页面以一张连续的纵向系统图展开。各区块之间不靠大面积留白切断，而由坐标轴、导轨、信号线和边界框连接，形成“同一台机器逐步完成”的感觉。

### 1. 顶部系统栏

- 使用一条极薄的固定或半固定状态栏，展示 `PERSONAL AGENT LAB / SYSTEM MAP / VERSION` 等系统身份。
- 左右角落使用十字准星、裁切标记和比例尺，建立工程图纸边界。
- 导航不做传统胶囊按钮；它应像系统坐标或设备标签，悬停时出现短橙线、编号或状态变化。
- 滚动过程中可让版本号、当前章节代号或系统状态更新，但不可造成布局跳动。

### 2. Hero：系统点火

#### 构图

- 左侧约占 40%，用超大中文标题形成四到五行强节奏：`把 Agent / 做成真正 / 能工作的 / 系统。`
- “Agent”与主要结论使用安全橙，其余文字使用暖纸色；行距紧，字面占据画面主体。
- 标题下方放一段短立场说明，只回答“我关心什么”，不堆砌技能。
- 右侧约占 60%，展示 Agent 核心及 `CONTEXT / MEMORY / TOOLS / EVIDENCE / BOUNDARY & CONSTRAINTS` 模块。
- 中央 Agent 是唯一高亮实体，外围模块以细线、箭头和动作词连接，如 `LOAD / RECALL / ACT / VERIFY / EXECUTE / RECORD`。
- 首屏左右不是两个互不相干的列：标题边界、信号线与核心装置必须交叉或共用一条系统基线，让文字与机器成为同一构图。

#### 进入动效

1. 外框、坐标和刻度先被快速绘制出来，像设备通电后完成自检。
2. 大标题按“结构块”落位，不逐字做花哨动画；橙色结论最后锁定。
3. 外围模块沿导轨从边界进入，各自停靠并出现端口。
4. 信号线从外围模块向中央传播，中央 Agent 在首次脉冲到达时点亮。
5. 最后出现一次短促的 `START PULSE` 波形，系统状态由 `STANDBY` 转为 `RUNNING`。

动效重点是“装配关系”，而不是透明度淡入。每个部件都应有来源、路径、停靠点和完成状态。

### 3. 项目编舞舞台

#### 结构

- 项目区是一个桌面端 pinned 的滚动剧场，上方固定章节标题 `PROJECT STAGE / 项目编舞舞台`。
- 通过三段明确状态 A、B、C 展示三个项目或同一能力体系的三个视角。
- 左侧是当前状态编号、项目标题、简述和状态；中央是主视觉系统；右侧是纸色说明板，解释系统如何工作、关键原则和证据。
- 每一幕都必须有主导视觉，不使用三个同质化卡片。
- 在 1440px 宽度下，当前场景的标题、主视觉和说明板必须完整可见，不得依赖横向裁切制造效果。

#### 状态 A：节点拓扑

- 中央 Agent 与 Context、Memory、Tools、Evidence、Boundary 节点组成拓扑图。
- 进入时节点从散落状态沿路径归位；连接线按依赖顺序点亮。
- 滚动推进时，信息包从输入端经过上下文、工具和证据节点，最终返回 Agent。
- 重点表达：Agent 不是单一模型调用，而是由上下文、能力、约束和证据共同组成的系统。

#### 状态 B：数据路径

- 主视觉变为从 `INPUT` 到 `ROUTE & PLAN`、`CHECK & VALIDATE`、`TOOLS EXECUTION`、`WRITEBACK & RECORD` 的横向执行链。
- 一条橙色脉冲沿路径运行，每到一个模块都触发内部机械动作：开闸、盖章、切换、写回。
- 下方的 fallback 路径在异常条件出现时展开，进入 `FAIL SAFE` 或 `HALT & ALERT`。
- 重点表达：系统能够规划、检查、调用工具、记录结果，也知道何时停止。

#### 状态 C：证据输出与责任边界

- 主视觉变为证据生产线：候选输出依次经过验证、评分、来源链接、边界标记、审计记录和交付。
- 每完成一步，证据包内新增一个可见项目；来源与责任边界通过线缆锁定。
- 最终 `EVIDENCE PACKAGE` 与 `RESPONSIBILITY BOUNDARY` 并列确认。
- 重点表达：可交付结果需要证据链、可追溯性和明确责任，而不只是生成文本。

#### 场景转场

- A → B：拓扑节点沿水平导轨被拉直，连接关系重排为执行管线。
- B → C：执行管线上的记录模块展开，形成验证与证据生产线。
- 转场时保留少量连续元素，例如中央 Agent、橙色脉冲和边界线，让用户理解这是同一系统的不同工作状态。
- 禁止仅使用 opacity、轻微 y 位移或整体缩放作为主要场景切换。

### 4. 时间日志：活的时间轨迹

#### 构图

- 使用横向时间信号或类似示波器的连续轨迹贯穿整个区块。
- 每个事件按相对时间落位，包含时间戳、事件名称、状态和证据提示。
- 当前事件使用橙色波形与 `RUNNING` 标识；历史事件退为暖灰；未来或待复核事件保持低对比轮廓。
- 时间轴必须让人感到“当前仍在发生”，而不是简历表格。

#### 动效

- 波形随滚动轻微向前推进，当前事件的脉冲保持活跃。
- 事件到达中心扫描线时，内容展开并完成一次确认；离开后收束为时间节点。
- 进度信号只沿实际阅读进度前进，不做无限高速循环。
- 可加入极低频率的扫描、游标闪烁或证据写入动作，但需要克制，避免成为背景噪声。

### 5. Finale：系统汇聚

- 结尾不重复 Hero 的大标题排版，而是让前文散落的导线、节点、证据和信号从页面边缘向中心收束。
- 中央形成一个稳定的 `AGENT SYSTEM` 边界，显示 `BOUNDARY LOCKED & CERTIFIED` 或等价的真实结论性状态。
- 左侧总结系统具备的可验证、可追溯、可交付、可审计属性；右侧呈现有限的系统指标或边界摘要。
- 指标只能表达设计原则或已能证明的状态，不能虚构百分比成绩。
- 联系与导航动作在系统锁定后出现，像下一次任务入口，而不是普通页脚链接。

#### 收束动效

1. 橙色信号回流至中央。
2. 外围模块沿轨道合拢并锁定。
3. 证据线与责任边界完成最后连接。
4. 状态从 `ASSEMBLING` 变为 `SYSTEM READY`。
5. 联系入口亮起，页面保持稳定，不再继续制造视觉高潮。

### 6. 响应式与无障碍

- 桌面：保持精密系统图与大范围轨道编舞，项目舞台可 pinned。
- 平板：减少外围刻度密度；主视觉与说明板改为上下关系；缩短 pinned 距离。
- 手机：取消复杂固定舞台，三个状态按顺序垂直展开；每幕只保留核心结构、主要路径和一段说明。
- `prefers-reduced-motion`：所有模块直接处于已装配状态；用静态高亮路径表示执行顺序；项目场景改为普通章节切换。
- 所有重要信息必须是可访问的真实文本，系统图中的装饰线和刻度不进入无障碍树。

### 方案优势与风险

优势：最贴合“可靠 Agent 系统”的定位，品牌辨识度强，首尾完整，能够把架构思考直接视觉化。  
风险：工程图细节过多时可能压迫内容；需要严格控制层级、动画数量和移动端降级；机械感必须服务于项目逻辑，不能沦为空洞装饰。

### 方案 01 深化稿：重型机械装配线

参考原图：`C:\Users\24719\AppData\Local\Temp\codex-clipboard-c4f4f227-dd5e-4edb-ad84-144061e2053e.png`

这张深化稿不是第三个独立方向，而是方案 01「机械编舞剧场」更具实体质感的执行版本。它将原方案中的二维系统蓝图进一步具象化为一条真实运行的机械装配线：金属机架、固定螺栓、滑轨、机械锁扣、穿孔记录纸和模块外壳共同组成页面世界。

#### 核心视觉变化

- 从“屏幕里的系统图”升级为“可以被组装的实体机器”。Context、Memory、Tools、Evidence 不再只是方框节点，而是具有重量、接口和锁定结构的独立硬件模块。
- 近黑底色继续作为主场景，暖纸色从阅读间隔升级为真实的工单、记录纸和标签材料。
- 安全橙集中用于 Agent 核心、连接锁扣、进行中状态和关键执行路径，使视觉注意力始终落在当前工作状态。
- 材质以哑光黑金属、磨损边缘、压印纸张、细网格和少量工业警示纹为主。避免高亮镀铬、霓虹灯、玻璃面板或科幻飞船式界面。
- 所有拟物细节都必须服从信息层级；在正常阅读距离下，标题、项目名称、状态和主要说明先于机械纹理被看见。

#### Hero：Agent 核心装配台

- 左侧延续超大中文宣言，使用暖白与橙色交替切分“把 Agent 做成真正能工作的系统”。标题保持块状、紧凑和强硬，不添加渐变或光效。
- 标题旁与画面边缘加入坐标、测量刻度、模式和状态，例如 `SYS_MODE: ASSEMBLY`、`STATUS: STANDBY`，建立装配前的待机时刻。
- 右侧主体是一台十字形 Agent 装配台：中央为橙色 Agent 核心，上方 Context、左侧 Memory、右侧 Tools、下方 Evidence。
- 四个模块通过可见机械臂、线缆、导轨与中央核心连接。每个模块有双语名称、模块 ID 与少量真实职责信息。
- 底部用一条简短逻辑公式收束：`CORE: CONTEXT + MEMORY + TOOLS + EVIDENCE`，并以“可靠执行且留有证明”作为结果方向。

Hero 的建议装配顺序：

1. 坐标网格、边界框与外部机架完成自检。
2. 中央 Agent 核心从装配台内部升起，但暂不点亮。
3. 四个外围模块沿各自导轨滑入，并用机械锁扣固定。
4. 橙色接口逐一闭合，线缆出现短促张力反馈。
5. 中央核心通电，状态由 `STANDBY` 切换至 `ASSEMBLY` 或 `RUNNING`。

#### Project Stage：三工位装配轨道

- 三个项目不再表现为普通内容卡，而是安装在同一条重型横向轨道上的三个工作工位。
- 每个工位由黑色金属框架、顶部滑轮、纸质任务单和下部媒体窗口构成；三个工位共享连续导轨和动力结构。
- 左右两端分别标记 `INPUT` 与 `OUTPUT`，三个工位之间用橙色方向箭头建立明确流程。
- 顶部编号 `01 / 02 / 03`、状态代号和项目模块标签保持固定，让用户在复杂画面中始终知道当前位置。

三个工位的语义建议：

1. `STATE: PLAN / 设计系统`：展示目标拆解、上下文建模、接口定义和约束设定。下部媒体窗口呈现真实架构图或项目界面。
2. `STATE: BUILD / 工具链接入`：展示工具注册、能力映射、安全边界和调用验证。下部窗口可呈现工具、节点或真实执行素材。
3. `STATE: RUN / 可靠执行`：展示任务执行、结果校验、证据留存和反馈闭环。下部窗口呈现真实日志、代码或运行结果。

滚动编舞建议：

- 舞台进入时，整条轨道先启动，三个空工位从边缘进入并停靠。
- 当前工位被提升到阅读位置，纸质任务单由夹具放下，下部媒体舱随后开启。
- 从工位 01 到 02 时，代表上下文或设计产物的实体件沿轨道移动，并在第二工位完成工具接口装配。
- 从工位 02 到 03 时，工具模块闭合，执行信号通过橙色连接件传递到第三工位。
- 第三工位完成后，结果从 `OUTPUT` 端退出，同时生成一条可追溯证据记录进入下一章节。
- 转场的主角是“产物沿生产线发生变化”，不是三张卡片轮流变亮。

#### Live Time Log：卷轴式证据日志

- 时间日志采用横向穿孔纸卷结构，左右有机械滚轴，中间是一张持续向前输送的暖纸色记录带。
- 顶部设置 `EVIDENCE SCANNER`，扫描器固定在中心；记录纸经过它时被读取、确认和盖章。
- 每条记录包括序号、真实时间信息、模块、任务、事件描述、状态、进度和证据 ID。二维码仅在确实存在可访问证据链接时使用，不能作为装饰性假数据。
- 当前事件使用橙色标题和描边状态章；已确认、已完成使用黑色或低饱和印章；所有状态同时配文字，不能只靠颜色区分。
- 纸张穿孔、折痕和滚轴用于表现“持续记录”，但正文区域必须平整、清晰，不使用妨碍阅读的透视和纹理。

时间日志的建议动效：

1. 记录带随页面滚动缓慢经过中心扫描器。
2. 当前记录对准扫描口时暂时停顿，扫描线从左至右读取。
3. 状态从待处理切换为进行中、已确认或已完成，随后出现一次克制的盖章动作。
4. 证据链完整时，底部 `LOG INTEGRITY` 与 `CHAIN` 状态被点亮。
5. 低动态模式直接显示已排版的三至五条记录，不移动纸带、不执行盖章动画。

#### Finale：稳定系统重新合拢

- 结尾左侧不是简单重复 Hero，而是以三行结论总结：`系统稳定。证据闭环。可持续协作。`
- 中央再次出现 Agent 装配台，但此时四个模块全部锁定，接口亮起，机械臂从“正在装配”变为“稳定承载”。这与 Hero 形成明确的前后状态对照。
- 右侧 `SYSTEM INFO` 只列真实可说明的系统信息：系统名称、构建模式、核心模块、证据链状态、运行状态和下一步入口。
- 主行动按钮采用设备命令式文案，例如 `READY FOR NEXT MISSION`，但点击结果必须对应真实联系方式或项目入口。
- 页脚保留装配线结束标记、短品牌声明与少量系统刻度，形成机器生产线停止后的余韵。

Finale 的建议收束顺序：

1. 时间日志最后一条证据通过扫描并进入系统。
2. Context、Memory、Tools、Evidence 四个模块完成最终锁定。
3. 中央 Agent 核心由运行橙切换为稳定橙，外围运动停止。
4. 左侧三行结论逐项确认，右侧系统信息更新为就绪状态。
5. 下一步入口点亮，页面进入安静且稳定的完成态。

#### Web 实现边界

- 机械质感优先通过 CSS 边框、层叠面板、纹理资源和少量经过压缩的图像建立，不应把整页做成一张不可访问的大图。
- 标题、项目说明、状态和联系方式必须保留为 HTML 文本；机械框架和装饰标记可作为非语义图层。
- 关键模块需使用可响应布局重新排布，不能依靠整张桌面图等比缩小到手机。
- 1440px 桌面画面必须完整容纳三工位核心内容；若信息密度不足，可通过纵向分幕解决，不得裁切第三工位。
- 使用纹理时控制对比度与文件体积，避免金属噪点损害中文正文清晰度或显著拖慢首屏。
- 所有机械运动都需要稳定的结束状态；交互完成后不保持无意义的持续抖动、旋转或闪烁。

---

## 方案 02：编辑电影切片

### 一句话概念

把首页拍成一部关于 Agent 系统的短片：首屏是开机取景，项目是三个镜头段落，时间日志是一卷持续记录的场记纸，结尾停在决定性的 `SYSTEM READY` 最终帧。

### 整体气质

这个方向比方案 01 更强调作者性、节奏和叙事。它借用摄影监视器、时间码、胶片边框、场记板、画幅安全区和硬切镜头的语言，把项目经历组织成“被观察、被记录、被确认”的制作过程。

系统结构仍然存在，但不以完整工程蓝图贯穿全页，而是以不同镜头语言呈现。画面需要有电影海报般的主次关系：有时是巨大标题，有时是压倒性的主画面，有时是安静的纸质记录。

### 1. 顶部取景器状态栏

- 顶栏显示 `PERSONAL AGENT LAB`、章节号、`STARTUP`、时间码与 `REC` 状态。
- 四角保留画幅安全区和取景器标记，右上角橙色录制点是全页唯一持续状态提示。
- 当前滚动章节可映射为 `SCENE 01–03` 或时间码推进，让用户感到页面正在被记录。
- 导航入口应像监视器功能键或片段索引，避免传统网站导航样式。

### 2. Hero：开机镜头

#### 构图

- 左侧使用不规则但稳定的巨大标题堆叠，让白色与橙色字块像电影海报标题一样交错。
- 标题不追求每行整齐对齐，而是通过字宽、缩进和颜色制造镜头节奏。
- 旁边保留摄影参数、焦距、ISO、帧率或时间码式装饰，但数量要少且不伪装成真实项目数据。
- 右侧是一个大取景框：中央 Agent 方块被多层画幅框包围，四角分布 System、Memory、Reliability、Tools 等模块。
- 坐标线与构图辅助线汇聚到中央，使 Agent 像正在被校准的拍摄主体。

#### 进入动效

1. 黑场短暂停留，`REC` 点亮。
2. 取景器安全框与中心十字出现，像镜头获得信号。
3. 标题以两到三次硬切进入，不做逐字弹跳。
4. 右侧模块像调整焦点一样从模糊轮廓变为清晰边界，中央 Agent 被连续画幅锁定。
5. 时间码开始推进，完成首屏开机。

### 3. Projects：三段镜头舞台

#### 构图

- 项目区采用不对称电影分镜布局，而不是等宽卡片网格。
- 项目 01 占据左侧大画幅，是建立镜头；项目 02、03 在右侧上下排列，作为近景与细节镜头。
- 每个项目都有场景编号、项目类型、标题、标签、角色、状态和 `ENTER` 入口。
- 主画面可使用项目真实截图、抽象系统渲染或已有视觉素材；不能用与项目无关的库存图冒充成果。
- 边框底部可加入胶片齿孔、帧编号与镜头定位标记，强化连续片段感。

#### 项目 01：建立镜头

- 使用最大画幅，先让用户看到项目的核心系统或代表性对象。
- 标题与角色信息覆盖在画面边缘，不遮挡关键内容。
- 滚动进入时，画面从宽镜头缓慢推进至关键细节，同时标签像字幕条一样依次锁定。

#### 项目 02：结构近景

- 使用建筑式裁切、系统界面局部或结构化图像表现知识与记忆系统。
- 画面沿一条明确轴线移动，露出不同层级，配合上下文、记忆、检索等真实项目信息。
- 运动应像摄影机横移或切换焦点，而不是整个卡片漂浮。

#### 项目 03：操作与证据蒙太奇

- 使用多帧 contact sheet 展示工具调用、执行结果、审计证据或过程截图。
- 当前帧被橙框选中，随后选择框在数个关键帧之间移动，形成“查看素材并确认结果”的动作。
- 每个缩略帧都需要与项目真实内容相关，避免纯装饰性重复。

#### 场景转场

- 用硬切、遮片、画幅门、接触印样切换和时间码跳转建立节奏。
- 重要转场可以让上一镜头的画幅边界成为下一镜头的分割线，保持空间连续性。
- 主画面内部必须发生构图变化：推镜、横移、焦点转移、帧选择或素材重排，不能只淡入淡出。
- 1440px 桌面宽度下，所有标题与关键画面处于安全区内，不裁掉项目主体。

### 4. Time Log：场记纸与胶片记录

#### 构图

- 使用一列连续的场记纸条或胶片记录，每条代表一次实践事件。
- 左侧是胶片孔、序号、状态贴纸和时间码；中央暖纸区域记录事件标题、模块和简述；右侧是信号图、场景编号及证据状态。
- 当前事件的纸条更亮，带橙色 `进行中` 标记；已完成事件压有确认章；待确认事件降低对比但保持可读。
- 纸条之间的夹子、装订线和卷轴只作为结构线索，不做过度拟物。

#### 动效

- 滚动时记录带沿垂直轨道移动，中心扫描线逐条读取事件。
- 当前条目进入扫描位置后，右侧波形开始记录，状态从空白变为进行中或完成。
- 已完成事件出现一次短促盖章动作；时间码停止在真实对应状态，不伪造精确日期。
- 动画应传达“过程被持续记录”，而不是制作无限循环的传送带。

### 5. Finale：最终帧

- 结尾做成真正的电影终场：左侧圆形准星显示 `END`，中央大画幅显示 `SYSTEM READY`，右侧是一块简化场记板。
- 中文副标题给出最终判断，例如“架构就绪，系统可用”，但措辞必须与当前真实完成度一致。
- 联系方式位于最终帧下方，像片尾的下一步动作：邮件、预约、其他真实可用入口。
- 页面不再回到 Hero 的中文巨型标题，而以英文系统状态和安静、确定的画面完成收束。

#### 收束动效

1. 项目片段缩回 contact sheet。
2. 时间码加速推进后停在最终帧。
3. 画幅门合拢，中央 `SYSTEM READY` 清晰显影。
4. 录制点熄灭或从 `REC` 变为 `READY`。
5. 联系入口留在可交互状态，背景停止运动。

### 6. 响应式与无障碍

- 桌面：保留不对称分镜、时间码和取景框关系。
- 平板：项目 01 保持大画幅，项目 02、03 改为横向连续片段，减少外围摄影参数。
- 手机：所有项目改为纵向镜头序列；每个镜头保持 4:5 或适合内容的稳定画幅；胶片孔与时间码退为局部提示。
- `prefers-reduced-motion`：取消推镜、快速硬切和卷动；直接显示各镜头的最终构图；场记条按普通列表阅读。
- 时间码和摄影参数属于视觉辅助，不承担唯一信息；录制状态不能只依赖颜色表达。

### 方案优势与风险

优势：视觉冲击与叙事感强，项目素材容易成为主角，节奏更像作品而不是系统说明书；最终帧具有明确记忆点。  
风险：电影语言若使用过多会掩盖 Agent 系统的可靠性论证；需要足够真实且质量统一的项目素材；硬切和推镜必须严格照顾眩晕敏感与低性能设备。

---

## 两个方案的核心差异

| 维度 | 方案 01：机械编舞剧场 | 方案 02：编辑电影切片 |
| --- | --- | --- |
| 核心隐喻 | 系统被装配、运行和验收 | 系统被拍摄、剪辑和记录 |
| 首屏高潮 | 模块接线并点火 | 取景器开机并锁定主体 |
| 项目组织 | 三个系统状态连续变形 | 三个电影镜头分段叙事 |
| 主运动模型 | 导轨、停靠、接线、脉冲、锁定 | 硬切、推镜、横移、对焦、选帧 |
| 时间表达 | 连续信号轨迹与事件节点 | 场记纸、胶片记录与时间码 |
| 最终收束 | 模块汇聚为稳定系统边界 | 停在 `SYSTEM READY` 最终帧 |
| 最强价值 | 直接证明系统工程能力 | 强化作品叙事与作者风格 |
| 主要风险 | 信息密度和机械细节过载 | 形式感压过系统逻辑 |

## 选择建议

- 如果首页第一目标是让招聘经理和高级工程师迅速相信“这是一个能设计可靠 Agent 系统的人”，优先选择方案 01。
- 如果现有项目拥有足够强、足够统一的真实视觉素材，并希望首页更像一部有导演意识的作品短片，可选择方案 02。
- 不建议把两个方案平均混合。可以在选定主方向后借用另一个方案的一项局部机制，但全页必须保持单一隐喻和一致运动语法。

## 选定方向后的实施顺序

1. 先确认最终使用的真实项目内容、媒体素材、角色、状态与链接。
2. 制作桌面端关键帧：Hero、三个项目状态、时间日志、Finale。
3. 定义动效状态机、滚动区间和 reduced-motion 静态结果。
4. 实现结构与样式，再接入内部场景动画。
5. 在 `1440×900` 或 `1440×1024` 验证首屏、项目不裁切和最终收束。
6. 检查平板、手机、键盘导航、横向溢出与低动态模式。
7. 运行 lint、build 和浏览器 QA；仅在明确要求时提交或推送。
